rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write to all collections
    // This is for development only - update for production!
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 6, 30);
    }
  }
}
